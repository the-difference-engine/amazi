<%= form_for @location do |f| %>
    Address : <%= f.text_field :address%> <br>
    Water Type : <%= f.select :water_type, ['Filtered', 'Tap', 'Natural Sources'], :include_blank => true %> <br>
    <%= f.submit 'Add location' %>
<% end %>
